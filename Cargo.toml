[package]
name = "libradicle"
version = "0.1.0"
description = "Embeddable Radicle facade: profile + in-process node + storage reads, one crate for desktop (napi) and iOS (uniffi) bindings"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
radicle = { path = "../heartwood/crates/radicle" }
radicle-node = { path = "../heartwood/crates/radicle-node" }
getrandom = "0.4"
git2 = { version = "0.21", default-features = false }
radicle-signals = { path = "../heartwood/crates/radicle-signals" }
log = "0.4"
thiserror = "2"

[dev-dependencies]
anyhow = "1"
env_logger = "0.11"

[profile.release]
lto = "thin"
