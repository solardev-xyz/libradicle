[package]
name = "libradicle-napi"
version = "0.5.0"
description = "Node.js (napi-rs) binding over libradicle — the Freedom Browser desktop path, replacing radicle-httpd"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[lib]
# libradicle.node — loaded by Node via a thin JS loader.
crate-type = ["cdylib"]

[dependencies]
libradicle = { path = ".." }
radicle = { git = "https://github.com/solardev-xyz/heartwood", rev = "394aa12d00855f09eb07638221993b4514682f6e" }
napi = { version = "3", default-features = false, features = ["napi8"] }
napi-derive = "3"
serde_json = "1"

[build-dependencies]
napi-build = "2"
